#include <stdio.h>

int main(){

    int ini, fim, aux, i;
    printf("Valor inicial e final: ");
    scanf("%i %i", &ini, &fim);

    printf("Primos: ");
    
    for(int i = ini; i <= fim; i++){
        if(i)
    }
        
    return 0;
}